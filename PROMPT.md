# Claude Code に渡すプロンプト

`CLAUDE.md` と同じフォルダで `claude` を起動し、下の枠の中をそのまま貼る。

---

## 1回目：デプロイ

```
このフォルダは Python 学習アプリの静的サイト。まず CLAUDE.md を読んで、
「絶対に破らない4つの約束」と「既知の制約」を把握してほしい。

今回のゴールは公開して、記述モードが実環境で動くことを確認すること。
手元では実行ボタンが押せない状態で、原因はブラウザのサンドボックスか
file:// による Pyodide の取得失敗だと踏んでいる。

やってほしいこと:

1. python3 verify.py を通す。0 でなければ先に進まず原因を報告する
2. ローカル確認。python3 -m http.server を立てて
   http://localhost:8000/pyplay.html を開き、Pyodide が読み込めるか確認する。
   失敗したらブラウザのコンソールに出ているエラーをそのまま見せてほしい
3. git リポジトリとして初期化し、GitHub に push する。
   リポジトリ名は pydrill、Public。.nojekyll を必ず含める
4. GitHub Pages を有効化する（main / root）。gh CLI が使えるなら
   gh コマンドで、使えないなら私がブラウザでやる手順を出す
5. 公開 URL を教えてほしい。あわせて /pyplay.html を開いて
   Pyodide が動いたかどうかを報告する

Pyodide が公開環境でも読み込めない場合は、まだ何も変更しないで報告だけしてほしい。
CDN が遮断されているなら、Pyodide 一式をリポジトリに同梱して
cdn.jsdelivr.net を経由しない形に切り替える案があるが、
10MB 増えるので判断は私がする。
```

---

## 2回目以降：開発

```
CLAUDE.md を読んでから作業を始めてほしい。

今回やりたいのは <やること>。

守ってほしい手順:
- データを足したり直したら、必ず python3 verify.py を通す。終了コード 0 が条件
- pydrill.html はオフラインのまま維持する。CDN 依存を足さない
- 辞書の見出しは「〜したい」の形にする。変数名は kansu / kansu1 / kansu2 で固定
- 私は Python の初学者なので、新しい構文を使うときは
  それが辞書のどのパターンに当たるか、または新規なら追加すべきかを言ってほしい
```

---

## 使えるサブエージェント

`.claude/agents/` に2つ置いてある。Claude Code が自動で選ぶが、明示的に呼んでもいい。

| エージェント | 役割 | 呼ぶ場面 |
|---|---|---|
| `output-verifier` | 宣言された出力と実行結果を照合し、不一致を差し戻す | データを足した / 直した直後。**省略不可** |
| `drill-author` | 範囲を渡すと問題データを生成する | 問題や辞書の見出しを増やすとき |

使い方の例:

```
辞書に「辞書（dict）を扱う」分類を足したい。
drill-author で見出しを4つ作って、そのあと output-verifier にかけてほしい。
```

---

## 引き継ぎ時点の状態

- 組み立てドリル 46問（基礎18 / 実戦28）
- 辞書 7分類 / 30見出し / 79パターン
- 記述問題 18問（pro 版のみ）
- 検証 143件すべて実行照合済み、`verify.py` 終了コード 0
- 未デプロイ。作者の環境で記述モードの実行ボタンが押せない状態
