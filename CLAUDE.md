# CLAUDE.md — Python 引き出しトレーニング

## このプロジェクトの目的

作者は Python の初学者。目的は知識の暗記ではなく、**「やりたいこと」から書き方を即座に取り出せる状態**を作ること。スラスラ解ける状態を「引き出しがある」と呼んでいる。

この目的から出てくる設計判断があり、それを守ることがこのリポジトリの価値の大部分を占める。

---

## 絶対に破らない4つの約束

### 1. 出力は必ず実行して確かめる

宣言した実行結果は、すべて実際に Python を走らせて照合している。`verify.py` がその工程を自動化してある。

```
python3 verify.py              # pydrill-pro.html
python3 verify.py pydrill.html
```

**問題やパターンを1件でも足したら、必ずこれを通す。** 終了コードが 0 でなければコミットしない。

エラー辞書（`ERRORS`）も同じ規律の下にある。**宣言したエラーが本当にその型・その文言で出るか**と、**直した形が本当に直って期待の出力を出すか**を全件実行して照合する。文言は「含まれるか」で見る（Python が上がると `Did you mean:` のような補足が末尾に付くことがあるため）が、**型は完全一致**——ここがズレたら別のエラーを教えていることになる。

LLM は `9 ** 0.5` を `3`、`-7 // 2` を `-3` と平気で書く。どちらも間違い（`3.0` と `-4`）。間違った出力を載せた教材は、間違いを覚えさせる。ここは推論で埋めてはいけない領域。

### 2. `pydrill.html` はオフラインで動き続ける

単体で完結した1ファイルであること。**起動と、46問・辞書の閲覧に通信を要さない**。作者はこの性質を過去の学習アプリ（`tango-player`、G検定サイト）でも一貫して守っており、電波のない場所で開けることが要件。

**2026-09-14 に約束の文言を緩めた**（要件は変えていない）。旧: 「フォント読み込み以外の外部通信を足さない」→ 新: 「**オフラインの核＋任意の Python**」。`pydrill.html` にも共有ランタイム `PyRun` を載せ、辞書の「動かす」と出題の「いじってみる」を**押した瞬間にだけ** Pyodide を取りに行くようにした。押さなければ通信は一切起きない。

**壊してはいけないのはこの構造**：Pyodide は必ず動的ロード（起動時に読まない）／取得に失敗しても46問と辞書は無傷で動き、実行機能だけが丁寧に失敗する。pro 版の記述モードと同じ型で、遮断テスト（`route('**/pyodide/**').abort()`）で毎回確認すること。

インタプリタは `PyRun` が1つだけ持ち、辞書・出題・記述モードで使い回す（辞書で一度起こせば記述モードは即時になる）。

### 3. 辞書は「やりたいこと」から引く

教科書は「`%` とは何か」から引く。この辞書は逆で、「循環させたい」「余りごと出したい」から引く。見出しは必ず `〜したい` の形にする。記号から引く索引は教科書がすでに持っているので作る価値がない。

### 4. 変数名は `kansu` / `kansu1` / `kansu2` で固定

作者の明示的な指定。名前が固定だとパターン間の差分だけが目に入る。学習用の置き場所としての約束なので、辞書と記述問題の中では変えない。

---

## ファイル構成

| ファイル | 役割 | 通信 |
|---|---|---|
| `index.html` | 入口。3つのアプリへのリンク | 不要 |
| `pydrill.html` | 組み立てドリル46問 + 辞書30見出し + その場実行。**核はオフライン** | 実行機能を押した時のみ |
| `pydrill-pro.html` | 上記 + 記述モード18問（Pyodide 採点） | 実行機能・記述モードのみ |
| `pyplay.html` | コードと入力を書いて実行する試行錯誤場 | 初回のみ |
| `python-patterns.md` | 辞書のテキスト版26件 | — |
| `verify.py` | 全データの実行照合 | — |

`pydrill-pro.html` は `pydrill.html` を土台に生成した派生物だが、**いまは両方が独立した実ファイル**。46問や辞書を直すときは両方に同じ変更を入れる必要がある。これは既知の負債（下記「次にやること」参照）。

---

## データ構造

### QUESTIONS — 組み立て型と出力予測型（46問）

```js
// 組み立て型
{id:'m2', cat:'数を扱う', tier:2,
 q:'添字が範囲を超えても先頭に戻るようにする',
 ctx:["names = ['A', 'B', 'C']", 'for i in range(4):', '    print(names['+SLOT+'])'],
 tokens:['i','%','len','(','names',')'],   // この順が正解
 extra:['//','+'],                          // まぎれ込ませる余分なチップ
 alt:[[...]],                               // 別解（任意）
 out:'A\nB\nC\nA',                          // プログラム全体の実行結果
 exp:'解説（HTML可）'}

// 出力予測型
{id:'x1', cat:'落とし穴', tier:2, type:'predict',
 q:'この出力はどれ', code:['print(-7 // 2)'],
 choices:['-4','-3','-3.5','3'], ans:0, out:'-4', exp:'...'}
```

`ctx` は前提コード。`SLOT`（`▁`）を書いた位置に組み立てた行が入る。行そのものでも、行の一部でもよい。`ctx` が文字列の場合は「前提」バッジ表示のみで、`out` は `null`。

**判定はトークン配列の完全一致**。表示は `joinCode()` が整形するので、空白の見た目は正誤に影響しない。`extra` は `tokens` と同じ文字列を含めてはいけない（`verify.py` が検出する）。

### DICT — 辞書（7分類 / 30見出し / 79パターン）

```js
{cat:'入力を受け取る', items:[
  {id:'i2', head:'1回の入力が2行で、別々の変数に入れたい',
   use:'どこで使うか', trap:'落とし穴（HTML可）',
   v:[{l:'2行をそのまま', c:'kansu1 = input()\n...', i:'abc\n123', o:'abc 123'}]}]}
```

`i` が標準入力、`o` が実行結果。

### 見出しカード（2026-09-14）

辞書30見出しとエラー辞書18件は、どちらも **2列のカードグリッド**で並ぶ（`#d-list` / `#e-list`）。
スロットは **media → header(eyebrow + title) → body → metadata → footer** の順。組み立ては `cardFace()`。

守るべき4点（崩すと典型的な事故が戻る）:
- **chin（フッタ上の死んだ余白）**: カードは `display:flex; flex-direction:column`、フッタは `margin-top:auto`。
  グリッドが等高に伸ばしてもフッタの上に隙間が出ない。実測で全カード 0px を確認している。
- **角丸のはみ出し**: カードに `overflow:hidden`。media は自前の角丸を持たない。
- **不正な入れ子**: カード全体が1個の `<button>` で、中身は `<span>`。`<div>` を `<button>` に入れたり、
  カードを `<a>` で包んで中にリンクを入れると不正な HTML になる。実測で違反0件。
- **padding の置き場**: padding は各スロットが持つ。media だけは持たず、縁まで到達させる。

- **media は画像ではなくコードの断片**。このアプリに画像は1枚も無く、飾りの絵を足す意味が無い。
  `white-space:pre` + `overflow:hidden` で切り取る（画像の `object-fit:cover` に相当）。
- **展開時は `grid-column:1/-1` で2列をまたいで全幅**になり、カード面は見出しだけに畳む。
  アコーディオンを残したまま格子を成立させるための形。
- **列数**: `minmax(150px,1fr)` で 360px 幅（Android に多い）でも2列。320px だけは読めなくなるので1列に落ちる。
  **3つの辞書すべて**（`#d-list` / `#e-list` / `#a-list`）に同じグリッドを当てる——`#a-list` を書き忘れて
  原子辞書だけ1列になる事故を実際にやった。

### 配色の方針（2026-09-15 更新）

- **色数を絞る**: 面＝紫グレー1色、強調＝黒、アクセント＝紫1色。正誤の `--good` / `--bad` だけが例外
  （意味を運ぶ色なので残す）。飾りの teal は**全廃**した。戻さない。
- **強調＝黒**: `--sunken: hsl(276,22%,7%)`。コード面・出力・解説・落とし穴・原子の役割は全部この黒い凹み面に置く。
  面（L15%）との差がはっきり出るので、凹みの錯覚も効く。黒の上でも ink 16.0 / code 12.8 / pencil 5.6 で全部 AA 以上。
- **紫は `hsl(281,66%,62%)`**。**L58% 付近は谷**で、白文字 4.26 / 濃文字 4.13 と**どちらも AA を落とす**。
  濃くするなら L62% まで（濃文字 4.78 で AA 維持）か、思い切って L54% 以下にして**白文字に切り替える**（白 4.91）。
  中途半端な濃さにしないこと。

### ATOMS — 原子辞書（53原子 / 170例 / 12クラス）

記号ひとつを「**役割 → なにができるか → 例 → 落とし穴**」で引く。
3つの辞書は**入口が違う**だけで、同じ「手元にある手がかりから引く」原則の別の面:

| 辞書 | 何から引くか |
|---|---|
| DICT | やりたいこと（〜したい） |
| ATOMS | 目の前のコードに写っている**記号** |
| ERRORS | 出ている**エラー文** |

```js
{cat:'計算する', items:[
  {id:'a9', sym:'%', head:'% は余り', role:'割った余りを返す',
   use:'なにができるか（HTML可）', trap:'落とし穴（HTML可）',
   v:[{l:'ラベル', c:'コード', i:'入力(任意)', o:'出力'}]}]}
```

**形は DICT と完全に同じ**（cat / items[ id, head, use, trap, v[] ]）に `sym` と `role` を足しただけ。
だから `verify.py` は `check_dict(ATOMS)` をそのまま流用して全例を実行照合できる。増やす時もこの形を崩さない。

- カードの media は**記号そのもの**を大きく置く（`cardFaceSym`）。辞書/エラーが media にコードを置くのと同じ枠。
- `toggleRun` と `copyCode` は DICT と ATOMS の両方を id で探す。**id は辞書間で衝突させない**
  （DICT= i/t/r/m/j/s/o 始まり、ATOMS= a、ERRORS= e）。
- 実行例のマークアップは `varHtml()` に共通化してある。辞書と原子辞書はこれを共用する。
- **クラス（分類）はチップ列で見せる**（`#a-chips` / `renderAtomChips`）。件数つきで、押すとそのクラスだけに絞る。
  12クラス: 文字と数の書き方 / 計算する / 代入する / 比べる / 論理でつなぐ / 所属と繰り返し /
  流れを変える / 関数とモジュール / 入れ物の記号 / 形をつくる記号 / 組み込みの道具 / 特別な値。

### タブ（実行例の切り替え・2026-09-15）

例を縦にずらっと並べるのをやめ、**タブで畳んだ**。`varsTabs()` が組み立て、`wireTabs()` が配線する。
辞書と原子辞書の両方が使う。

ref: `ant.design/components/tabs` の **line タイプ**（インクバーが active の下を動く）。
**Ant の文書はキーボード操作に触れていない**ので、そこは WAI-ARIA の tabs パターンで補ってある:

- `role="tablist"` / `role="tab"` / `role="tabpanel"`、`aria-selected`、`aria-controls`、`aria-labelledby`
- **roving tabindex**（active だけ `0`、他は `-1`）。Tab キーでタブ列を素通りできる
- **← →** で巡回、**Home / End** で端へ。移動先にフォーカスを移し `scrollIntoView` で見切れを防ぐ
- 非 active パネルは `hidden` 属性で隠す（`display:none` を直書きしない）

**壊さない点**: インクバーは `offsetLeft`/`offsetWidth` から位置を出すので、**カードが開いてから**
組み立てること（`display:none` の中では 0 になる）。いまは `classList.toggle('open')` が
`buildAtomBody()` より先に走るのでこの順序が保たれている。

**詳細度の罠**: 旧アコーディオンの `＋/−` は `.ditem.open .dhead::after` で置いていた。
カード化のとき `.dhead::after{content:none}` だけでは**詳細度で負けて消えず**、開いた見出しの右に
`−` が残った。`.ditem.open .dhead::after` も明示的に `none` にしてある。

### ERRORS — エラー辞書（18件 / 6分類）

```js
{id:'e7', cat:'種類が合わない',
 err:'TypeError',                                   // 例外の型。verify は完全一致で見る
 msg:'can only concatenate str (not "int") to str', // 実際に出る文言。verify は部分一致で見る
 why:'原因（HTML可）',
 bad:"kansu = input()\nprint(kansu + 1)", bi:'5',   // これを実行すると そのエラーが出る
 fix:"kansu = int(input())\nprint(kansu + 1)", fi:'5', fo:'6',  // 直した形と その出力
 note:'解説（HTML可）'}
```

本編の辞書が「やりたいこと」から引くのに対し、これは **目の前に出ているエラー文から引く**。
学習者が持っている手がかりがエラー文そのものなので、索引をそれに合わせている（約束3の例外ではなく、同じ原則の別の入口）。

各項目に **いじり場**（`toggleErrRun`）が付く。壊れた形で開き、「直した形」ボタンで往復できる。
`PyRun.panel` の `setCode()` / `setExpect()` がその差し替え口。

**バージョン差の確認済み**: 18件すべて、ローカル Python 3.11.15 と Pyodide 3.13.2 で文言が完全一致
（2026-09-14 実測）。Python を上げた時は必ず `verify.py` を通し直すこと。

**実行ラッパの `EOFError` は本物の英文を残す**。以前は日本語だけに差し替えていたため、学習者が
`EOF when reading a line` を一生見られなかった。エラー辞書の目的に反するので
`EOF when reading a line — 入力データが足りません（…）` の形にしてある。ここを日本語だけに戻さない。

### WRITE — 記述問題（18問、pro 版のみ）

```js
{id:'w1', cat:'記述', tier:3, type:'write',
 q:'2行で与えられる数を足して表示する',
 st:'',            // エディタの初期値（前提コード）
 in:'3\n7',        // 標準入力
 o:'10',           // 期待する出力
 m:'模範解答',      // 正解後に表示
 e:'解説'}
```

採点は標準出力の照合。**行末の空白と前後の空行は無視する**（`normOut`）。`end=' '` を使った解法も通る。複数の正解を許すのは意図的な設計。

---

## PyRun — 共有 Python ランタイム（Web Worker 版 / 2026-09-14）

`pydrill.html` / `pydrill-pro.html` / `pyplay.html` の3枚に同じブロックで入っている。

```js
PyRun.ensure()                      // Worker を立てて Pyodide を読む（2回目以降は即返る）
PyRun.run(code, stdin, onChunk)     // → Promise<{out, err, cancelled}>／onChunk(文字列) が届いた順に呼ばれる
PyRun.cancel()                      // 実行中の Worker を terminate（無限ループでも必ず止まる）
PyRun.panel({code, stdin, expect, showStdin})  // 編集＋実行＋中断＋一致判定のDOMを返す
PyRun.booted() / PyRun.busy() / PyRun.version()
```

**なぜ Worker なのか（戻すな）**: メインスレッドで動かすと `time.sleep()` がブラウザごと固まり、
出力は最後にまとめてしか出ない（画面を描き直す隙が無い）。Worker に追い出すと sleep はワーカー側
だけを止めるので、`print()` のたびに postMessage が飛び、出力欄に1行ずつ流れる。実測で
`time.sleep(2)` のループが 2001ms 間隔で1つずつ届くことを確認している。無限ループを
`terminate()` で切れるようになったので「中断」ボタンも成立した。

**設計の要点**
- Python のラッパは Worker のソースに**埋めず**、`postMessage` で文字列として渡す（二重エスケープの沼を避ける）。
- Worker のソースも Python ラッパも**配列 `.join('\n')`** で組む（入れ子テンプレートリテラルのパースエラー回避。`node --check` で事前検出）。
- CDN は `new URL(CDN, location.href).href` で絶対化してから渡す（Blob Worker は相対パスを解決できない）。
- `cancel()` は Worker ごと捨てる。次の実行で建て直す（キャッシュから読むので速い）。
- 出力は Worker 側で `setStdout({batched})` を使い、溜め込まない。例外のトレースバックだけ `done` で返し、`onChunk` にも流して DOM と `out` を一致させる。

**配線**
- 辞書: 「動かす」→ `toggleRun()` が `PyRun.panel` を `.dvar` に挿す。`expect` にデータの `o` を渡す。
- 出題: 答え合わせ後の「いじってみる」→ `toggleQuizRun()`。組み立て型は `ctx` の `SLOT` を正解トークン列で置換した**プログラム全体**、出力予測型は `code` が種。
- 記述モード: `judgeWrite()` が `PyRun.run()` を使い、採点前に途中経過を流す。
- 試行錯誤場: `run()` が同じ経路。起動時には読まず「実行」を押した時に読む。

---

## 記述モードの実行系

Pyodide（WebAssembly 版 CPython 3）を `cdn.jsdelivr.net/pyodide/v0.28.3/` から読む。

実行は `RUNNER` という Python ラッパ経由。やっていること:

- `sys.stdin` に入力データを差し込み、`input()` を再定義する（`input()` 系と `sys.stdin` 系の両方が動く）
- `linecache` に書いたコードを登録し、トレースバックが**書いた行を指す**ようにする
- 実行ラッパ自身のスタックフレームを除去し、`code.py` のフレームだけ残す
- 入力不足は `EOFError: 入力データが足りません` と日本語で返す

**このラッパは JS のテンプレートリテラル内にある Python コード**なので、`\n` などのエスケープが二重になる。編集したら、ブラウザが受け取る文字列を復元してから Python で実行し、正常系・例外・入力不足の3系統で確認すること。ここは黙って壊れる。

---

## 既知の制約（直そうとする前に読む）

- ~~無限ループでタブが固まる~~ **2026-09-14 解決**。Pyodide を Web Worker に移し、`PyRun.cancel()`（実行中は主ボタンが「中断」になる）で止められる。中断後は Worker を建て直すので、そのまま次の実行ができる
- **記述問題は `print` でカンニングできる。** 期待出力を画面に出しているので `print('3.14')` で通る。自習ツールなので許容している
- **f文字列の書式指定（`:.2f`、`:>5`）は組み立て型で出題できない。** `f'{kansu:.2f}'` が1チップになって作業が消える。文字入力型という4つ目の型が必要
- localStorage 保存なので、端末をまたいで記録は共有されない。キーは `pydrill_v2` / `pyplay_v1`
- 初回起動はスマホで十数秒かかる

---

## pyblock — ブロックで組んでから書く（骨組み実装済み）

`pyblock.html`。設計は `docs/BLOCKS.md`。組む → 実行 → 写経 → 判定 まで通る。
**実測 14タップ＋関数名1回**で `def tasu(a,b) / return a+b / print(tasu(2,3))`（手打ち56文字）。
要点:

- **目的**: 構造と記法を分けて片方ずつ確定させる。スマホで10分打ってエラー→原因が構造か記法か
  切り分けられない、という詰み方を無くす。
- **4段**: ブロックで組む → 実行して動く → **ブロックを見ながら Python で打つ** → 出力が一致。
  **③で生成された Python を見せない**（見せると書き写しになり構造への確信が使われない）。
- **2層**: 文＝新規ブロック、**式＝既存ドリルのチップ方式を流用**。式まで入れ子にすると複雑さが倍になる。
- **ドラッグは使わない**。挿入点にタップで入れる。360px で掴んで運ぶのは遅い。
- **レールは左**（作者が左利き＝拾う手が左・読む面が右）から縦に弾いて探す。**設定で左右を入れ替え可**。
  札は短い名前、**意味はホームで引く**。
- **ホーム＝ブロックの辞書**（既存3辞書と同じデータ形＝verify.py の検証面）、**組み立て面は隔離**。
- **サンドボックス**。何を作るかは自由。お題は任意。作ったものは localStorage に保存。
- **関数は第1版から入れる**。名前を付けられることが手応えを決める。名前だけキーボードを許す。
- **段差は字下げでなく左の色帯**。360px で右に寄せると3段で詰む。
- **置き場所は独立した1枚 `pyblock.html`**。既存2枚は120KB超＋重複なので入れない。
- **verify.py**: データに生成結果 `code` を持たせて実行照合。JS 生成器とのズレは起動時に自己テスト
  （生成器を2つ持つ二重管理を避けるため）。**骨組みの段階ではデータが無いので検証対象なし**——
  ホームのブロック辞書を作る時に入れる。
- **「⇤ 外へ」は消さない**。挿入点を親の次へ出すボタン。無いと `def` の外に出すだけで
  「行を選ぶ→⇤→スロット」の3タップを食う（16→14タップの差はこれ）。
- **ドラッグとタップは併用**（2026-09-17）。タップ挿入を消さないこと——速さの生命線であり、
  **WCAG 2.2 の 2.5.7 がドラッグに単一ポインタの代替を必須にしている**。キーボード操作も同じ理由。
- **レールのドラッグ判定は「レールの外へ出たか」**。縦移動で判定してはいけない（レールは縦スクロールし、
  下部の札を面の上部へ運ぶと縦移動の方が大きい。実測 dx=182 / dy=367 で横優先判定は不成立だった）。
- **面の外へ出すと削除**。ドラッグ中は「はなすと消えます」を出す。
- **数はブロックに直接書き込む**（キーパッド）。`1` の札を探させない。小数・負数も入る。
- **グリップは 24px 以上**（WCAG 2.5.8 最小ターゲット）。18px にしない。
- **写経画面は自動で字下げする**（2026-09-18）。素の textarea は Python の練習にならない。
  `:` のあと +1段 / `return` などのあと −1段 / `else:` を打ち終えた瞬間に −1段 / 行頭 Backspace は4つまとめて。
  **Tab を字下げに使うならキーボードの抜け道が要る**（WCAG 2.1.2）——Esc の直後の Tab だけは素通しし、
  その作法を画面に常時出す。
- **面の寄り引きは `transform:scale` ではなく `zoom`**（2026-09-18）。スクロール量と
  `getBoundingClientRect` が素直に付いてくるのでドラッグの当たり判定を書き直さずに済む。
  `.canvas{touch-action:pan-x pan-y}` でページのピンチ拡大を面の上だけ止める。
- **`::before` で当たり判定を広げるときは要素に `position:relative` を付ける**。
  無いと絶対配置の親を基準に解決され、隣のボタンを丸ごと覆う（ズームつまみで実際に起きた）。
- **無限キャンバスにはしない**。Python はインデントが構造なので、位置に意味を持たせると
  「どれが本体か」が曖昧になる。次の段階は**島**（本体1つ＋下書き複数、Scratch のハット規則）。
- **`render()` は場所を控えてから作り直す**（2026-09-18）。DOM を丸ごと作り直す設計なので、
  前後で **レールの scrollTop と `data-fk`（フォーカス中の要素）** を控えて戻すこと。
  やらないと (1) 札を押すたびレールが 3,660px の先頭に戻り (2) フォーカスが body に落ちて
  キーボード操作が2手目で死ぬ。ブロックの同一性は `_k`（`mkBlock`/`keyUp` が振る）。
- **`pointercancel` は削除ではない**。OS のジェスチャ横取り・ペンが浮いた・通知が降りた、で飛ぶ。
  `dragEnd` に繋ぐと、指を離してもいないのにブロックが消える。中止（`dragCancel`）に繋ぐこと。
- **確認ダイアログではなく取り消し**（`snap()`/`undo()` 40段＋localStorage 自動保存）。
  `prompt`/`confirm` はソースから0件。入力は自前シート `#ask` に統一（OS ダイアログは外観を破る）。
- **実行前のガードは「穴」だけでなく「式として成立しているか」を見る**（`exprWhy`/`walkWhy`）。
  このアプリの存在理由は「エラーが出たら原因は記法だけだと断言できること」。
  止めたら **その行まで面を送る**。画面外だと何も起きていないように見える。
- **道具列は画面の下**（2026-09-18）。行を選ぶ→動かす は主要ループ。上に置くと親指から 700px 離れる。
  **上の帯は1本だけ**（タブ＋設定アイコンを同じ行に）。2本にすると面が 444px まで痩せた。
- **`import` は札**（`t:'imp'`）。`random.*` / `time.*` / `math.*` の札は **import が面に無い間は沈める**。
  押したら「先に import random を入れる」と言う。実行前ガードも同じ判定を持つ。
- **見本は `SAMPLES`**。追加したら **実機の Pyodide で走ることを必ず確認**する
  （ローカルミラーの作り方は DEPLOY.md ではなく、CDN を `sed` で差し替えて別ポートで配るだけ）。
- **分類の色は5色＋中立**（2026-09-17）。ながれ=橙 / だす・いれる=青緑 / いれもの=青 /
  かんすう=桃 / しき=紫 / じゆう=中立。**分類は `STMT[].c` の1か所**で決まり、
  レールの欄・印の色・面の行の帯とキーワードの色が全部そこから出る。
  **色は唯一の手がかりにしない**（印の形と見出しの言葉でも分かれる／WCAG 1.4.1）。
  6色とも面・レール・地のどこに載せても 4.5:1 超。**字下げの棒は中立色**にすること——
  色を持たせると分類の帯と見分けがつかない。
- **レールの並びはユーザが決める**。↑↓ ボタン（ドラッグには代替が要る／WCAG 2.5.7）。
  保存された並びに後から増えた欄を足して返す（更新で欄が消えないように）。
  **「いつもの」の集計は読み込み時と設定を閉じた時だけ凍らせる**——毎描画で作り直すと
  指の下で並びが動く。
- **拡大させない策に `user-scalable=no` は使わない**（WCAG 1.4.4 違反・iOS は無視する）。
  効くのは `touch-action:manipulation`（ダブルタップ拡大を止める）＋
  入力欄 16px 固定（iOS のフォーカス時オートズーム）＋**画面内で文字サイズを変えられること**。
  レール幅は rem にして文字と一緒に伸ばす。
- **閉じたシートは `visibility:hidden`**。transform で画面外に逃がしただけだと
  Tab が画面の外のボタンを巡る。畳んだレールも同じ。
- **レールは macOS のソースリスト型**（2026-09-17）。平たい行＋分類ごとの印＋sticky な見出し。
  **見出しは節ごとに `.rsec` で囲む**——囲まないと通り過ぎた見出しが上に積み上がる。
  **選択の塗りは付けない**（レールは行き先ではなく道具箱。押したら挿入されて終わり）。
  **`backdrop-filter` は使わない**（tango と同じ理由でスクロールが死ぬ）。明度差だけで材質を分ける。
- **`.railnow`＝「いま押したらどこに入るか」**。レールの先頭に貼り付く1行。
  見出しの sticky 位置は `--now-h`（実測高さ）に合わせる。入らない分類は `.off` で薄くするが押せなくはしない。
- **列は `order` ではなく `grid-column` で決める**。`--pane-w:0` の幅では pane が `display:none` になり、
  自動配置の項目が2つに減って**右利き設定でレールが 1fr を食い、面が 16px に潰れていた**。
- **980px 以上で結果は右の柱**。iPad 横で面を縦いっぱい使うため。
- **未実装**: ホーム（ブロックの辞書）／保存（localStorage に名前を付けて一覧）／お題（任意）。

## 次にやること（優先順）

1. **デプロイ**（`PROMPT.md` 参照）。まず記述モードが実環境で動くことを確認する。作者の手元では実行ボタンが押せない状態で、原因はプレビューのサンドボックスか `file://` の可能性が高い
2. **`pydrill.html` と `pydrill-pro.html` の二重管理をやめる。** データを `data.json` に外出しし、両方がそれを読む形にするのが素直。ただしオフライン版の単体完結性（約束2）を壊さないこと。`fetch` はローカルファイルで失敗するので、ビルド時に埋め込む方式にする
3. **文字入力型の問題タイプ**を足して、f文字列の書式指定を出題できるようにする
4. **誤答ログから SRS。** `ST.stats` に `{c, a}` が貯まっているので土台はある。作者が過去に作った G検定サイトに SRS 実装があるので、そこから移植する意図があった
5. 辞書の見出しを増やす。増やしたら `verify.py`
